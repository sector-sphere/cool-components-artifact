import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import * as ImageTextCard from './image-text-card.stories.js';
import ImageTextCardStories from './image-text-card.stories.js';

<Meta of={ImageTextCardStories} />

# Card Component

CardComponent is a reusable component that serves the purpose of...

<Canvas>
    <Story component={ImageTextCard} />
</Canvas>

## Props

<ArgsTable of={ImageTextCard} />

## Usage

Here's an example of how you can use MyComponent in your application:

```jsx
<TextCardDesktop prop1={value1} prop2={value2} />
```