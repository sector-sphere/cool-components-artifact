import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import * as ProgressBar from './progress-bar.stories.js';
import { progressBarConstants } from './progress-bar.constants.js';

<Meta of={ProgressBar} />

# Button Component

ProgressBar is a reusable component that serves the purpose of...

<Canvas>
    <Story component={ProgressBar} />
</Canvas>

## Props

<ArgsTable of={ProgressBar} />

## Usage

Here's an example of how you can use MyComponent in your application:

```jsx
<ProgressBar prop1={value1} prop2={value2} />
```